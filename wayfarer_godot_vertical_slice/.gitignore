.godot/
*.tmp
*.translation

# Old local-only export folder from PR #388 - the canonical export now
# lives in export/web/ and IS committed so Cloudflare can serve it at
# /godot/ on the next deploy. See WEB_DELIVERY.md.
web_build/
