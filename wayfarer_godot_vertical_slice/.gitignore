# Godot editor state and imported binary caches are generated locally.
.godot/

# Local export credentials are machine-specific.
export_credentials.cfg

# Local Godot Web export output. Regenerated by tools/export_web.sh and
# packaged for browser review (see WEB_DELIVERY.md).
# Binary .pck/.wasm/.js artifacts do not belong in git.
web_build/

# Review artifacts are generated by tools/package_itch_web.sh.
artifacts/
wayfarers-tale-godot-web.zip
web_build.zip

# macOS AppleDouble sidecars appear when this project is edited on some
# external drive formats. They should never be committed.
._*
**/._*

*.tmp
*.translation
