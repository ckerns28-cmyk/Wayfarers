extends SceneTree

# The G-22 capture wrapper composes the final OVI-1 review package from the
# refreshed current-branch proof artifacts because this gate is a formal review
# package rather than a new runtime placement pass.


func _initialize() -> void:
	print("G-22 OVI-1 capture is driven by capture_g22_ovi1_runtime_screenshots.ps1.")
	quit(0)
